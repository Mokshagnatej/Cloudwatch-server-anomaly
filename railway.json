{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd DAY-15 && ./build.sh"
  },
  "deploy": {
    "startCommand": "cd DAY-15/backend && source venv/bin/activate && gunicorn --workers 1 --threads 2 --bind 0.0.0.0:$PORT main:app"
  }
}
