{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "./build.sh"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn --workers 1 --threads 2 --bind 0.0.0.0:$PORT app:app"
  }
}
